1. 这个项目是数据库可重复脚本导出工具，可用于生成初始化数据，全量数据，增量数据以及建表脚本等操作
2.todo:
1) 后续需要增加行内的日志系统对接 以及其他必要系统的对接配置
2）数据库密码需要改成加密的： 目前先不加密。但是自定义数据源需要先加上
3）mybatis pagehelper 多线程的配置都加上
4）后期开发尽量严格按照领域模型
5) infra说明：比如table是服务模块，最外面方gatewayimpl, 里面是方rpc或者数据库调用的mapper
